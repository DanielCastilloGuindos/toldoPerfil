---
export const prerender = true;

import Layout from "@/layouts/Layout.astro";
import { LOCATIONS_DATA, type LocationData } from "@/consts/locations";

export async function getStaticPaths() {
    return LOCATIONS_DATA.map((municipio) => {
        // Generates toldos-loeches, toldos-madrid, etc.
        const paramValue = municipio.slug.replace("toldos-", "");
        return {
            params: { municipio: paramValue },
            props: { municipio },
        };
    });
}

interface Props {
    municipio: LocationData;
}

const { municipio } = Astro.props;

// Dynamic Meta tags with transactional calls
const metaTitle = `Instalación de Toldos en ${municipio.municipioName} | Precio de Fábrica`;
const metaDescription = `${municipio.descriptionLocal} Fabricamos e instalamos toldos cofres, toldos brazo extensible y pérgolas bioclimáticas. ¡Presupuesto gratis hoy!`;
const canonicalUrl = `https://toldoperfil.es/toldos-${municipio.slug.replace("toldos-", "")}`;

// Prevent thin content by tailoring based on geographical zone
const isPriorityZone1 = municipio.priorityZone === 1;
const zoneBadgeText = isPriorityZone1 ? "Zona de Servicio Preferente (Cercanía Extrema)" : "Servicio Oficial Comunidad de Madrid";
---

<Layout 
    title={metaTitle} 
    description={metaDescription}
    canonical={canonicalUrl}
>
    <!-- Structured Data JSON-LD LocalBusiness + AreaServed -->
    <script type="application/ld+json" is:inline set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "LocalBusiness",
        "name": "Toldo Perfil",
        "image": "https://toldoperfil.es/img/og.jpeg",
        "@id": `https://toldoperfil.es/toldos-${municipio.slug.replace("toldos-", "")}#localbusiness`,
        "url": `https://toldoperfil.es/toldos-${municipio.slug.replace("toldos-", "")}`,
        "telephone": "+34639535038",
        "priceRange": "$$",
        "address": {
            "@type": "PostalAddress",
            "streetAddress": "Sector M-4, Nave 37",
            "addressLocality": "Velilla de San Antonio",
            "postalCode": "28891",
            "addressRegion": "Madrid",
            "addressCountry": "ES"
        },
        "geo": {
            "@type": "GeoCoordinates",
            "latitude": 40.3541,
            "longitude": -3.4883
        },
        "openingHoursSpecification": {
            "@type": "OpeningHoursSpecification",
            "dayOfWeek": [
                "Monday",
                "Tuesday",
                "Wednesday",
                "Thursday",
                "Friday"
            ],
            "opens": "09:00",
            "closes": "19:00"
        },
        "areaServed": [
            {
                "@type": "AdministrativeArea",
                "name": municipio.municipioName
            },
            {
                "@type": "AdministrativeArea",
                "name": "Velilla de San Antonio"
            },
            {
                "@type": "AdministrativeArea",
                "name": "Comunidad de Madrid"
            }
        ]
    })} />

    <main class="min-h-screen bg-slate-50 text-slate-800 selection:bg-indigo-100 py-12">
        <div class="max-w-4xl mx-auto px-6 space-y-12">
            
            <!-- Breadcrumbs -->
            <nav class="text-xs text-slate-500 flex items-center gap-2">
                <a href="/" class="hover:text-indigo-600 transition-colors">Inicio</a>
                <span>/</span>
                <span class="text-slate-600">Instalación en {municipio.municipioName}</span>
            </nav>

            <!-- Hero Section / Header Block -->
            <header class="relative bg-white border border-indigo-100 rounded-3xl p-8 sm:p-12 overflow-hidden shadow-xl shadow-indigo-100/25">
                <div class="absolute -right-24 -top-24 w-64 h-64 bg-indigo-600/5 rounded-full blur-3xl"></div>
                
                <span class={`inline-block px-3 py-1 text-xs font-bold rounded-full mb-6 border ${
                    isPriorityZone1 
                    ? "bg-emerald-50 text-emerald-700 border-emerald-200" 
                    : "bg-indigo-50 text-indigo-700 border-indigo-200"
                }`}>
                    {zoneBadgeText}
                </span>

                <h1 class="text-3xl sm:text-5xl font-black tracking-tight text-slate-900 leading-tight">
                    Toldos y Pérgolas en <span class="text-indigo-600">{municipio.municipioName}</span>
                </h1>
                
                <p class="text-slate-600 text-base sm:text-lg mt-6 leading-relaxed max-w-2xl">
                    {municipio.descriptionLocal} Garantizamos una instalación impecable adaptada al clima de la zona con materiales de primera calidad fabricados en España.
                </p>

                <!-- Action Button -->
                <div class="mt-8 flex flex-wrap gap-4">
                    <a 
                        href="/#contacto" 
                        class="px-6 py-3.5 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold rounded-xl transition-all shadow-lg shadow-indigo-600/20 text-sm"
                    >
                        Solicitar Presupuesto Gratis
                    </a>
                </div>
            </header>

            <!-- Geographical Priority Hook (Anti-thin content) -->
            <section class="space-y-6">
                <h2 class="text-2xl font-bold text-slate-900 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                    Compromiso de Servicio Técnico y Cercanía
                </h2>

                {isPriorityZone1 ? (
                    <div class="bg-emerald-50/60 border border-emerald-100 rounded-2xl p-6 space-y-4">
                        <div class="flex items-center gap-3">
                            <span class="p-2 bg-emerald-100/60 text-emerald-700 rounded-lg">🚀</span>
                            <h3 class="text-lg font-bold text-slate-900">Instalador Directo de Cercanía</h3>
                        </div>
                        <p class="text-slate-600 text-sm leading-relaxed">
                            Al encontrarse {municipio.municipioName} en las cercanías de nuestro taller central de Velilla de San Antonio, podemos coordinar visitas técnicas personalizadas y ofrecerte un trato directo de fabricante, sin comisiones de intermediarios ni costes inaccesibles de desplazamiento.
                        </p>
                    </div>
                ) : (
                    <div class="bg-indigo-50/60 border border-indigo-100 rounded-2xl p-6 space-y-4">
                        <div class="flex items-center gap-3">
                            <span class="p-2 bg-indigo-100/60 text-indigo-700 rounded-lg">🛡️</span>
                            <h3 class="text-lg font-bold text-slate-900">Servicio de Instalación de Confianza</h3>
                        </div>
                        <p class="text-slate-600 text-sm leading-relaxed">
                            Ofrecemos soporte integral en toda la Comunidad de Madrid. Nos desplazamos a {municipio.municipioName} para realizar mediciones exactas con láser digital, asesorarte estéticamente y garantizar una instalación limpia y duradera.
                        </p>
                    </div>
                )}
            </section>

            <!-- Value Proposition Checklist -->
            <section class="grid grid-cols-1 md:grid-cols-3 gap-6">
                {municipio.puntosFuertes.map((punto, index) => (
                    <div class="bg-white border border-indigo-50/60 rounded-2xl p-6 flex flex-col justify-between hover:border-indigo-200 hover:shadow-md transition-all">
                        <div class="text-indigo-600 text-lg font-black mb-4">0{index + 1}</div>
                        <p class="text-sm text-slate-700 font-medium leading-relaxed">{punto}</p>
                    </div>
                ))}
            </section>

            <!-- Local Keywords / Solutions Showcase -->
            <section class="space-y-6">
                <h2 class="text-2xl font-bold text-slate-900">
                    Sistemas de Protección Solar que Instalamos
                </h2>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    {municipio.keywords.map((kw) => (
                        <div class="flex items-center gap-3 bg-white border border-indigo-50 rounded-xl p-4 hover:border-indigo-100 transition-colors shadow-sm">
                            <span class="w-3 h-3 rounded-full bg-indigo-600"></span>
                            <span class="text-sm font-semibold capitalize text-slate-700">{kw} en {municipio.municipioName}</span>
                        </div>
                    ))}
                </div>
            </section>

            <!-- Local SEO Footer Notice -->
            <footer class="border-t border-indigo-100 pt-8 text-center text-xs text-slate-400 leading-relaxed">
                Toldo Perfil &copy; {new Date().getFullYear()} - Fábrica central en Velilla de San Antonio. Especialistas homologados en protección solar y automatismos domóticos. Instalación oficial de toldos en {municipio.municipioName}.
            </footer>
        </div>
    </main>
</Layout>
